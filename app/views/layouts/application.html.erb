<!DOCTYPE html>
<html>
  <head>
    <title>RacingCars</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <ul class="navigation">
      <li class="navItems"><%= link_to "Home Page", root_path %></li>
      <% if !logged_in? %>
        <li class="navItems"><%= link_to "Login", login_path %></li>
      <% else %>
        <li class="navItems"><%= link_to "Sign Out", logout_path %></li>
      <% end %>
    </ul>
    <%= yield %>
  </body>
</html>
